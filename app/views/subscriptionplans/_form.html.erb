<%= form_with(model: subscriptionplan) do |form| %>
  <% if subscriptionplan.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subscriptionplan.errors.count, "error") %> prohibited this subscriptionplan from being saved:</h2>

      <ul>
        <% subscriptionplan.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.text_field :price %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
